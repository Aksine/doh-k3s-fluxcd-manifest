---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: navidrome-app-pvc
  namespace: default
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  # volumeName should be same as PV name
  volumeName: navidrome-app-pv
  storageClassName: csi-cephfs-sc