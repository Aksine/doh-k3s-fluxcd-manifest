---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: navidrome-lib-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: navidrome-lib-pv
  storageClassName: smb
