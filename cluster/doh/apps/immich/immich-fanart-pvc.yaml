---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: immich-fanart-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  volumeName: immich-fanart-pv
  storageClassName: smb
